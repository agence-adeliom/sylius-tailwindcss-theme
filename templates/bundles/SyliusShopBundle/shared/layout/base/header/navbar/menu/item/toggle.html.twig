{% set taxon = hookable_metadata.context.taxon %}
<div tabindex="{{ taxon.id }}" role="button" class="cursor-pointer">
    {{ taxon.name }}
    <i class="fa-solid fa-chevron-down max-lg:!hidden"></i>
</div>