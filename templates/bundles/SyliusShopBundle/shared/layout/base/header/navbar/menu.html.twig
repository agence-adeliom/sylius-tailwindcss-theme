<div js-menu>
    <div class="pointer-events-none bg-black/60 opacity-0 z-20 fixed w-screen h-dvh top-0 left-0 is-open:opacity-100 duration-300 ease-in-out is-open:pointer-events-auto lg:hidden" js-menu-backdrop></div>  
    <div class="w-screen h-dvh bg-white md:w-[60vw] lg:h-fit lg:w-full border-b border-neutral-300 max-lg:fixed z-[100] is-open:translate-x-0 max-lg:-translate-x-full duration-300 ease-in-out top-0 left-0">
        <i class="fa-regular fa-xmark lg:!hidden right-6 top-6 text-xl absolute" js-close-menu></i>
        {% if taxons|length > 0 %}
            <div class="container">
                <nav class="navbar" id="navbarNav">
                    {# <div class="offcanvas-header w-full">
                        <button type="button" class="btn-close" data-bs-dismiss="offcanvas" data-bs-target="#navbarNav" aria-label="Close"></button>
                    </div> #}

                    {# <div class="menu !flex w-fit  rounded-box lg:mx-auto" {{ sylius_test_html_attribute('menu') }}>
                        {% hook 'menu' with { taxons } %}
                    </div> #}
                    <div class="menu flex lg:flex-row lg:mx-auto lg:w-fit" {{ sylius_test_html_attribute('menu') }}>
                        {% hook 'menu' with { taxons } %}
                    </div>
                </nav>
            </div>
        {% endif %}
    </div>
</div>