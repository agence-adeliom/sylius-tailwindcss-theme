<div class="w-full border-b border-neutral-300">
    {% if taxons|length > 0 %}
        <div class="container">
            <nav class="navbar offcanvas-lg offcanvas-start offcanvas-wide p-0" id="navbarNav">
                {# <div class="offcanvas-header w-full">
                    <button type="button" class="btn-close" data-bs-dismiss="offcanvas" data-bs-target="#navbarNav" aria-label="Close"></button>
                </div> #}
                <div class="menu !flex w-fit lg:menu-horizontal  rounded-box lg:mx-auto" {{ sylius_test_html_attribute('menu') }}>
                    {% hook 'menu' with { taxons } %}
                </div>
            </nav>
        </div>
    {% endif %}
</div>
