{% set variant = item.variant %}
{% set product = variant.product %}

<div class="flex items-center gap-4">
    <div style="width: 6rem;">
        <div class="overflow-auto bg-light rounded-3 aspect-[3/4]">
            {{ component('sylius_shop:main_image', {
                product: variant.hasImages ? variant : product,
                class: "w-full h-full object-cover",
                filter: 'sylius_shop_product_small_thumbnail'
            }) }}
        </div>
    </div>
    <div>
        <div class="h6">
            <a class="underline" href="{{ path('sylius_shop_product_show', {'slug': product.slug, '_locale': product.translation.locale}) }}" {{ sylius_test_html_attribute('product-name', item.productName) }}>
                {{ item.productName }}
            </a>
        </div>

        <small class="" {{ sylius_test_html_attribute('product-variant-code', variant.code) }}>{{ variant.code }}</small>

        {% if product.hasOptions() %}
            <div {{ sylius_test_html_attribute('product-options') }}>
                {% for optionValue in variant.optionValues %}
                    <small class="" {{ sylius_test_html_attribute('option-name', optionValue.name) }}>
                        {{ optionValue.name }}: <span {{ sylius_test_html_attribute('option-value') }}>{{ optionValue.value }}</span>
                    </small>
                {% endfor %}
            </div>
        {% elseif item.variantName is not null %}
            <small class="" {{ sylius_test_html_attribute('product-variant-name') }}>
                {{ item.variantName }}
            </small>
        {% endif %}
    </div>
</div>
