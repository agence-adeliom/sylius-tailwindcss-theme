{% set product = hookable_metadata.context.product %}

<div class="font-bold lg:group-hover:text-primary" {{ sylius_test_html_attribute('product-name', product.name) }}>
    {{ product.name }}
</div>
