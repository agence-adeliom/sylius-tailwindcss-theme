{% import "@SyliusShop/shared/macro/money.html.twig" as money %}

{% set order = hookable_metadata.context.order %}

<div class="flex justify-between text-sm">
    <span class="text-base-content/70">{{ 'sylius.ui.items_total'|trans }}</span>
    <span id="sylius-shop-checkout-summary-items-total">{{ money.convertAndFormat(order.itemsSubtotal) }}</span>
</div>
