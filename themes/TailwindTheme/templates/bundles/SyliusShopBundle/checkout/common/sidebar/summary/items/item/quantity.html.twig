{% set item = hookable_metadata.context.item %}

<span class="badge badge-neutral badge-sm shrink-0" {{ sylius_test_html_attribute('item-subtotal-quantity', item.variant.product.name) }}>
    &times;{{ item.quantity }}
</span>
