{% import "@SyliusShop/shared/macro/money.html.twig" as money %}

{% set cart = hookable_metadata.context.cart %}

{% if cart.currencyCode is not same as(sylius.currencyCode) %}
    <div class="flex gap-2 border-t pt-3 mt-3">
        <div class="heading-5 pt-4 mt-4">{{ 'sylius.ui.base_currency_order_total'|trans }}:</div>
        <div class="ml-auto heading-5 pt-4 mt-4 text-right" {{ sylius_test_html_attribute('cart-base-grand-total') }}>{{ money.format(cart.total, cart.currencyCode) }}</div>
    </div>
{% endif %}
