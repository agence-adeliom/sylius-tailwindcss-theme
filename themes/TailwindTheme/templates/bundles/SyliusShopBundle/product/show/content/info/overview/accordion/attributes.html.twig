{% set product = hookable_metadata.context.product %}

{% if product.attributes|length > 0 %}
    <div class="collapse collapse-arrow border-b border-neutral-300 rounded-none">
        {% hook 'attributes' %}
    </div>
{% endif %}
