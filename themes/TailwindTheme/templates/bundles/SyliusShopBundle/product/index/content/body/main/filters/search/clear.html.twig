<a class="btn btn-ghost join-item" href="{{ path('sylius_shop_product_index', {'slug': app.request.attributes.get('slug')}) }}" {{ sylius_test_html_attribute('clear') }} aria-label="{{ 'sylius.ui.clear_filters'|trans }}">
    {{ ux_icon('tabler:x', {class: 'size-4'}) }}
</a>
