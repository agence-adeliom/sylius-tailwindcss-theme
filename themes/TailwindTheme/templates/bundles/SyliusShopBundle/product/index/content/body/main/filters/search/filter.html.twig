{% set products = hookable_metadata.context.products %}

<div class="join-item flex-1 [&_input]:input [&_input]:input-bordered [&_input]:w-full [&_input]:rounded-none [&_input]:focus:outline-none [&_input]:focus:border-base-300">
    {% for filter in products.definition.enabledFilters %}
        {{ sylius_grid_render_filter(products, filter) }}
    {% endfor %}
</div>
