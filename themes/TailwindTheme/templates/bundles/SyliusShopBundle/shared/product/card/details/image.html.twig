{% set product = hookable_metadata.context.product %}

<div class="mb-3">
    <div class="overflow-hidden rounded-lg aspect-[3/4] bg-base-200">
        {{ component('sylius_shop:main_image', {product: product, class: "w-full h-full object-cover transition-transform duration-500 ease-out group-hover:scale-105"}) }}
    </div>
</div>
