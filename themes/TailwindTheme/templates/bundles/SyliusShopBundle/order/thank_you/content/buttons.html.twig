{% set order = hookable_metadata.context.order %}

<div class="flex flex-col lg:flex-row justify-center gap-2 mt-4">
    {% if order.customer.user is not null %}
        {% hook 'buttons#customer' %}
    {% else %}
        {% hook 'buttons#guest' %}
    {% endif %}
</div>
