{% set order = hookable_metadata.context.order %}
{% set order_cannot_be_paid = not sylius_order_can_be_paid(order) %}

<div class="block">
    <button type="submit" class="btn btn-primary w-full{% if order_cannot_be_paid %} disabled{% endif %}" {{ sylius_test_html_attribute('pay-link') }} {% if order_cannot_be_paid %}disabled{% endif %}>
        {{ 'sylius.ui.pay'|trans }}
    </button>
</div>
